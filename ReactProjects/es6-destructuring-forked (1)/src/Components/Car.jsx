import React from "react";
import ReactDOM from "react-dom";

function Car(props) {
  <tr>
    <td>{props.model}</td>
    <td>{props.speed}</td>
    <td>{props.topColor}</td>
  </tr>;
}

export default Car;
